#!/usr/bin/env python

from distutils.core import setup

setup(
    name='dogm_py',
    version='0.0.1',  # specified elsewhere
    packages=[''],
    package_dir={'': '.'},
    package_data={'': ['dogm_py.so']},
)
